diff --git a/dist/providers/anthropic.js b/dist/providers/anthropic.js
index d4fc29b8c97264ae51b212bdf69b29f7e8001ad1..d6fda57454b0e0e5297f1f511c1327b960565c66 100644
--- a/dist/providers/anthropic.js
+++ b/dist/providers/anthropic.js
@@ -220,6 +220,16 @@ export const streamAnthropic = (model, context, options) => {
                         output.content.push(block);
                         stream.push({ type: "toolcall_start", contentIndex: output.content.length - 1, partial: output });
                     }
+                    else {
+                        const block = { type: "text", text: "", index: event.index };
+                        output.content.push(block);
+                        stream.push({
+                            type: "content_block",
+                            contentIndex: output.content.length - 1,
+                            block: event.content_block,
+                            partial: output,
+                        });
+                    }
                 }
                 else if (event.type === "content_block_delta") {
                     if (event.delta.type === "text_delta") {
diff --git a/dist/providers/google-gemini-cli.js b/dist/providers/google-gemini-cli.js
index bffeb6219f2aeb34d103afa175b19f8f61e2fe9f..cf07f1dd4b4817703b45afb6a893f2fa300e9eed 100644
--- a/dist/providers/google-gemini-cli.js
+++ b/dist/providers/google-gemini-cli.js
@@ -517,6 +517,16 @@ export const streamGoogleGeminiCli = (model, context, options) => {
                                     output.stopReason = "toolUse";
                                 }
                             }
+                            if (candidate?.groundingMetadata) {
+                                const block = { type: "text", text: "" };
+                                output.content.push(block);
+                                stream.push({
+                                    type: "content_block",
+                                    contentIndex: output.content.length - 1,
+                                    block: { type: "grounding_metadata", metadata: candidate.groundingMetadata },
+                                    partial: output,
+                                });
+                            }
                             if (responseData.usageMetadata) {
                                 // promptTokenCount includes cachedContentTokenCount, so subtract to get fresh input
                                 const promptTokens = responseData.usageMetadata.promptTokenCount || 0;
diff --git a/dist/providers/openai-responses-shared.js b/dist/providers/openai-responses-shared.js
index 38dc2c4c1124111ca6bdc165817c127b6a3726ab..9a7e9cacea6e318e6ab6b53380b9edb5d0865c11 100644
--- a/dist/providers/openai-responses-shared.js
+++ b/dist/providers/openai-responses-shared.js
@@ -1,4 +1,29 @@
 import { calculateCost } from "../models.js";
+async function* abortableStream(source, signal) {
+    if (!signal) {
+        yield* source;
+        return;
+    }
+    const iterator = source[Symbol.asyncIterator]();
+    const abortPromise = new Promise((_, reject) => {
+        if (signal.aborted) {
+            reject(new DOMException("Aborted", "AbortError"));
+            return;
+        }
+        signal.addEventListener("abort", () => reject(new DOMException("Aborted", "AbortError")), { once: true });
+    });
+    try {
+        while (true) {
+            const result = await Promise.race([iterator.next(), abortPromise]);
+            if (result.done)
+                break;
+            yield result.value;
+        }
+    }
+    finally {
+        iterator.return?.();
+    }
+}
 import { shortHash } from "../utils/hash.js";
 import { parseStreamingJson } from "../utils/json-parse.js";
 import { sanitizeSurrogates } from "../utils/sanitize-unicode.js";
@@ -221,7 +246,7 @@ export async function processResponsesStream(openaiStream, output, stream, model
     let currentBlock = null;
     const blocks = output.content;
     const blockIndex = () => blocks.length - 1;
-    for await (const event of openaiStream) {
+    for await (const event of abortableStream(openaiStream, options?.signal)) {
         if (event.type === "response.created") {
             output.responseId = event.response.id;
         }
diff --git a/dist/providers/openai-responses.js b/dist/providers/openai-responses.js
index 6ecbb7f6a2e9394905ae91e04a2860e195ccf6f3..75f30e9f12a6d9524dd907fd4e677a053a8952c8 100644
--- a/dist/providers/openai-responses.js
+++ b/dist/providers/openai-responses.js
@@ -70,6 +70,7 @@ export const streamOpenAIResponses = (model, context, options) => {
             await processResponsesStream(openaiStream, output, stream, model, {
                 serviceTier: options?.serviceTier,
                 applyServiceTierPricing,
+                signal: options?.signal,
             });
             if (options?.signal?.aborted) {
                 throw new Error("Request was aborted");
diff --git a/dist/types.d.ts b/dist/types.d.ts
index 91d3c54f9a6d1e599a8b8bd0e0d5f53b8bb51cda..033ad71a027238858a7086b060496b2c3625aba0 100644
--- a/dist/types.d.ts
+++ b/dist/types.d.ts
@@ -205,6 +205,11 @@ export type AssistantMessageEvent = {
     contentIndex: number;
     toolCall: ToolCall;
     partial: AssistantMessage;
+} | {
+    type: "content_block";
+    contentIndex: number;
+    block: unknown;
+    partial: AssistantMessage;
 } | {
     type: "done";
     reason: Extract<StopReason, "stop" | "length" | "toolUse">;
